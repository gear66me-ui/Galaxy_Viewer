# viewer-0019 — Locked milestone

This directory contains the approved Galaxy Viewer milestone `viewer-0019.py`.

## Protection rules

- Do not edit or overwrite the archived file.
- Do not import, invoke, patch, or execute the archived file from later viewers.
- Do not use the archived path as a runtime dependency.
- All future viewer revisions must be complete standalone files.
- Future development must begin from a separate working copy, never from this locked directory.

The original historical file at `viewer/viewer-0019.py` is also left unchanged so existing links and commit history remain intact.
